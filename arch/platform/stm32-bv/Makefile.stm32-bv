CONTIKI_TARGET_DIRS = .

CONTIKI_TARGET_SOURCEFILES += leds-arch.c platform.c

CONTIKI_SOURCEFILES += $(CONTIKI_TARGET_SOURCEFILES)

CFLAGS += -DUSE_STDPERIPH_DRIVER
CFLAGS += -DSTM32F40_41xxx

CLEAN += *.stm32-bv

CONTIKI_CPU=$(CONTIKI)/arch/cpu/arm/stm32f4
include $(CONTIKI_CPU)/Makefile.stm32f4
